import React from 'react';
import { connect } from 'react-redux';

const Setting = (props) => {
  return <>setting-works !!</>;
};

Setting.propTypes = {};
const mapStateToProps = (state) => ({});
export default connect(mapStateToProps, {})(Setting);
